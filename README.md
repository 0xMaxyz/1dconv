```bash
bun install
# after that, copy CalldataLib and its dependencies to either data/ directory or pass their address correctly
bun run src/cli.ts data/input/CalldataLib.sol data/output --run-tests
```
