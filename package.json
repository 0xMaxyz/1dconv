{
  "name": "conv",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun run src/cli.ts data/input/CalldataLib.sol data/output --run-tests",
    "test": "bun test",
    "build": "bun build --target bun ./src/cli.ts --outdir ./dist"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^22.13.14",
    "ts-node": "^10.9.2",
    "viem": "^2.24.1"
  },
  "peerDependencies": {
    "typescript": "^5.8.2"
  },
  "dependencies": {
    "ajv": "^8.17.1"
  }
}
